---
apiVersion: secrets-store.csi.x-k8s.io/v1alpha1
kind: SecretProviderClass
metadata:
  name: aws-secrets
  namespace: default
spec:
  provider: aws
  secretObjects:                              # [OPTIONAL] SecretObject defines the desired state of synced K8s secret objects
  - data:
    - key: username                           # data field to populate
      objectName: username                  # name of the mounted content to sync. this could be the object name or the object alias
    - key: password 
      objectName: password
    secretName: creds                     # name of the Kubernetes Secret object
    type: Opaque
  parameters:
    objects: |
      - objectName: creds #aws secret name in secret manager
        objectType: secretsmanager
